# scripting

#### set -e 

* this command is used in  instruct the script to exit immediately if any command within it returns a non-zero exit status, indicating an error

* this is useful for preventing scripts from continuing exicution when error occures

#### if[$# eq 0]

* No arguments provided

#### 
